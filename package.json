{
  "name": "subly",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && npx wrangler pages deploy dist"
  },
  "devDependencies": {
    "vite": "^6.0.0",
    "wrangler": "^3.114.0"
  }
}
